# Prefect orchestrator dependencies
prefect>=3.0.0

# Core dependencies needed for tasks (matching pyproject.toml / Prefect 3.x)
httpx>=0.27.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
python-dateutil>=2.8.0

# PDF processing dependencies  
docling>=2.0.0

# Search engine dependencies
opensearch-py>=2.4.0

# Database drivers
psycopg2-binary>=2.9.0
